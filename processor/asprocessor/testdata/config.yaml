as:
as/maxmind:
  providers:
    maxmind:
      database_path: /tmp/db
as/maxmind_record_context:
  context: record
  providers:
    maxmind:
      database_path: /tmp/db
as/invalid_providers_config:
  providers: "this should be a map"
as/invalid_source:
  providers:
    maxmind:
      database_path: /tmp/db
  context: not.an.otlp.context
as/invalid_source_attributes:
  providers:
    maxmind:
      database_path: /tmp/db
  attributes: []
as/custom_source_attributes:
  providers:
    maxmind:
      database_path: /tmp/db
  attributes: [client.address, source.address, custom.address]